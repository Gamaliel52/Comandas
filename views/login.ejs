<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Iniciar Sesión</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('/images/Fondo.png'); /* Fondo igual al de inicio */
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
        }
        .container {
            background-color: rgba(255, 255, 255, 0.9); /* Fondo blanco semitransparente */
            padding: 30px;
            border-radius: 10px;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        .btn-primary {
            font-size: 1.2rem;
            padding: 10px 20px;
            border-radius: 5px;
            background-color: #6c757d; /* Gris suave */
            border: none;
        }
        .btn-primary:hover {
            background-color: #5a6268; /* Gris más oscuro al pasar el mouse */
        }
        .btn-danger {
            font-size: 1rem;
            padding: 8px 15px;
            border-radius: 5px;
            position: absolute;
            top: 20px;
            left: 20px;
            background-color: #dc3545; /* Rojo suave */
            border: none;
        }
        .btn-danger:hover {
            background-color: #c82333; /* Rojo más oscuro al pasar el mouse */
        }
        .form-control {
            border: 1px solid #ced4da; /* Gris claro */
            background-color: #f8f9fa; /* Fondo gris muy claro */
            color: #495057; /* Texto gris oscuro */
        }
        .form-control:focus {
            border-color: #80bdff; /* Azul claro al enfocar */
            box-shadow: 0 0 5px rgba(128, 189, 255, 0.5); /* Sombra azul clara */
        }
        label {
            color: #495057; /* Texto gris oscuro */
            font-weight: bold;
        }
    </style>
</head>
<body>
    <a href="/" class="btn btn-danger">Atrás</a> <!-- Botón rojo para regresar -->
    <div class="container">
        <h1 class="mb-4">Iniciar Sesión</h1>

        <!-- Mostrar mensaje de error si existe -->
        <% if (typeof error !== 'undefined') { %>
            <div class="alert alert-danger" role="alert">
                <%= error %>
            </div>
        <% } %>

        <form action="/login" method="POST">
            <div class="mb-3">
                <label for="username" class="form-label">Usuario</label>
                <input type="text" class="form-control" id="username" name="username" required>
            </div>
            <div class="mb-3">
                <label for="password" class="form-label">Contraseña</label>
                <input type="password" class="form-control" id="password" name="password" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Iniciar Sesión</button>
        </form>
    </div>
</body>
</html>